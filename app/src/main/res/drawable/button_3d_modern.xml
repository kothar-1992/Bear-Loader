<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <layer-list>
            <!-- Shadow layer -->
            <item android:top="2dp" android:left="2dp">
                <shape android:shape="rectangle">
                    <gradient
                        android:angle="135"
                        android:startColor="@color/shadow_dark"
                        android:endColor="@color/shadow_light"
                        android:type="linear" />
                    <corners android:radius="28dp" />
                </shape>
            </item>
            <!-- Main button layer (pressed) -->
            <item android:bottom="2dp" android:right="2dp">
                <shape android:shape="rectangle">
                    <gradient
                        android:angle="135"
                        android:startColor="@color/button_background_pressed"
                        android:endColor="@color/button_background"
                        android:type="linear" />
                    <corners android:radius="28dp" />
                </shape>
            </item>
        </layer-list>
    </item>
    
    <!-- Normal state -->
    <item>
        <layer-list>
            <!-- Shadow layer -->
            <item android:top="4dp" android:left="4dp">
                <shape android:shape="rectangle">
                    <gradient
                        android:angle="135"
                        android:startColor="@color/shadow_dark"
                        android:endColor="@color/shadow_light"
                        android:type="linear" />
                    <corners android:radius="28dp" />
                </shape>
            </item>
            <!-- Main button layer (normal) -->
            <item android:bottom="4dp" android:right="4dp">
                <shape android:shape="rectangle">
                    <solid android:color="@color/button_background" />
                    <corners android:radius="28dp" />
                </shape>
            </item>
        </layer-list>
    </item>
</selector>
